<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ccit.mappers.LoginDao">
    <insert id="save" parameterType="com.ccit.pojo.Login">
       INSERT INTO login(ip,userid) VALUES (#{ip},#{userid})
    </insert>

</mapper>